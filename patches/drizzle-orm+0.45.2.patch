diff --git a/node_modules/drizzle-orm/pg-core/columns/timestamp.cjs b/node_modules/drizzle-orm/pg-core/columns/timestamp.cjs
index 207a6df..80ec01b 100644
--- a/node_modules/drizzle-orm/pg-core/columns/timestamp.cjs
+++ b/node_modules/drizzle-orm/pg-core/columns/timestamp.cjs
@@ -59,7 +59,7 @@ class PgTimestamp extends import_common.PgColumn {
     return value;
   }
   mapToDriverValue = (value) => {
-    return value.toISOString();
+    return value instanceof Date ? value.toISOString() : value;
   };
 }
 class PgTimestampStringBuilder extends import_date_common.PgDateColumnBaseBuilder {
diff --git a/node_modules/drizzle-orm/pg-core/columns/timestamp.js b/node_modules/drizzle-orm/pg-core/columns/timestamp.js
index 6ce67ac..ec77d8b 100644
--- a/node_modules/drizzle-orm/pg-core/columns/timestamp.js
+++ b/node_modules/drizzle-orm/pg-core/columns/timestamp.js
@@ -32,7 +32,7 @@ class PgTimestamp extends PgColumn {
     return value;
   }
   mapToDriverValue = (value) => {
-    return value.toISOString();
+    return value instanceof Date ? value.toISOString() : value;
   };
 }
 class PgTimestampStringBuilder extends PgDateColumnBaseBuilder {
diff --git a/node_modules/drizzle-orm/pg-core/db.cjs b/node_modules/drizzle-orm/pg-core/db.cjs
index 5863ec7..00d60af 100644
--- a/node_modules/drizzle-orm/pg-core/db.cjs
+++ b/node_modules/drizzle-orm/pg-core/db.cjs
@@ -99,21 +99,32 @@ class PgDatabase {
    */
   $with = (alias, selection) => {
     const self = this;
+    let isMaterialized;
+
     const as = (qb) => {
       if (typeof qb === "function") {
-        qb = qb(new import_query_builders.QueryBuilder(self.dialect));
+        qb = qb(new QueryBuilder(self.dialect));
       }
+
       return new Proxy(
-        new import_subquery.WithSubquery(
+        new WithSubquery(
           qb.getSQL(),
           selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}),
           alias,
-          true
+          true,
+          void 0,
+          isMaterialized
         ),
-        new import_selection_proxy.SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
+        new SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
       );
     };
-    return { as };
+
+    const materialized = (shouldBeMaterialized = true) => {
+      isMaterialized = shouldBeMaterialized;
+      return { as };
+    };
+
+    return { as, materialized };
   };
   $count(source, filters) {
     return new import_count.PgCountBuilder({ source, filters, session: this.session });
diff --git a/node_modules/drizzle-orm/pg-core/db.js b/node_modules/drizzle-orm/pg-core/db.js
index a0f5337..e6ef6f0 100644
--- a/node_modules/drizzle-orm/pg-core/db.js
+++ b/node_modules/drizzle-orm/pg-core/db.js
@@ -81,21 +81,32 @@ class PgDatabase {
    */
   $with = (alias, selection) => {
     const self = this;
+    let isMaterialized;
+
     const as = (qb) => {
       if (typeof qb === "function") {
         qb = qb(new QueryBuilder(self.dialect));
       }
+
       return new Proxy(
         new WithSubquery(
           qb.getSQL(),
           selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}),
           alias,
-          true
+          true,
+          void 0,
+          isMaterialized
         ),
         new SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
       );
     };
-    return { as };
+
+    const materialized = (shouldBeMaterialized = true) => {
+      isMaterialized = shouldBeMaterialized;
+      return { as };
+    };
+
+    return { as, materialized };
   };
   $count(source, filters) {
     return new PgCountBuilder({ source, filters, session: this.session });
diff --git a/node_modules/drizzle-orm/pg-core/dialect.cjs b/node_modules/drizzle-orm/pg-core/dialect.cjs
index b2c2a77..f09f33c 100644
--- a/node_modules/drizzle-orm/pg-core/dialect.cjs
+++ b/node_modules/drizzle-orm/pg-core/dialect.cjs
@@ -85,7 +85,15 @@ class PgDialect {
     if (!queries?.length) return void 0;
     const withSqlChunks = [import_sql2.sql`with `];
     for (const [i, w] of queries.entries()) {
-      withSqlChunks.push(import_sql2.sql`${import_sql2.sql.identifier(w._.alias)} as (${w._.sql})`);
+      let asKeyword = import_sql2.sql`as`;
+
+      if (w._.isMaterialized !== void 0) {
+        asKeyword = w._.isMaterialized
+          ? import_sql2.sql`as materialized`
+          : import_sql2.sql`as not materialized`;
+      }
+
+      withSqlChunks.push(import_sql2.sql`${import_sql2.sql.identifier(w._.alias)} ${asKeyword} (${w._.sql})`);
       if (i < queries.length - 1) {
         withSqlChunks.push(import_sql2.sql`, `);
       }
diff --git a/node_modules/drizzle-orm/pg-core/dialect.js b/node_modules/drizzle-orm/pg-core/dialect.js
index 31a019c..c9eef71 100644
--- a/node_modules/drizzle-orm/pg-core/dialect.js
+++ b/node_modules/drizzle-orm/pg-core/dialect.js
@@ -83,7 +83,15 @@ class PgDialect {
     if (!queries?.length) return void 0;
     const withSqlChunks = [sql`with `];
     for (const [i, w] of queries.entries()) {
-      withSqlChunks.push(sql`${sql.identifier(w._.alias)} as (${w._.sql})`);
+      let asKeyword = sql`as`;
+
+      if (w._.isMaterialized !== void 0) {
+        asKeyword = w._.isMaterialized
+          ? sql`as materialized`
+          : sql`as not materialized`;
+      }
+
+      withSqlChunks.push(sql`${sql.identifier(w._.alias)} ${asKeyword} (${w._.sql})`);
       if (i < queries.length - 1) {
         withSqlChunks.push(sql`, `);
       }
diff --git a/node_modules/drizzle-orm/pg-core/query-builders/query-builder.cjs b/node_modules/drizzle-orm/pg-core/query-builders/query-builder.cjs
index 5b43b0a..6f9ad74 100644
--- a/node_modules/drizzle-orm/pg-core/query-builders/query-builder.cjs
+++ b/node_modules/drizzle-orm/pg-core/query-builders/query-builder.cjs
@@ -36,21 +36,32 @@ class QueryBuilder {
   }
   $with = (alias, selection) => {
     const queryBuilder = this;
+    let isMaterialized;
+
     const as = (qb) => {
       if (typeof qb === "function") {
         qb = qb(queryBuilder);
       }
+
       return new Proxy(
-        new import_subquery.WithSubquery(
+        new WithSubquery(
           qb.getSQL(),
           selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}),
           alias,
-          true
+          true,
+          void 0,
+          isMaterialized
         ),
-        new import_selection_proxy.SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
+        new SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
       );
     };
-    return { as };
+
+    const materialized = (shouldBeMaterialized = true) => {
+      isMaterialized = shouldBeMaterialized;
+      return { as };
+    };
+
+    return { as, materialized };
   };
   with(...queries) {
     const self = this;
diff --git a/node_modules/drizzle-orm/pg-core/query-builders/query-builder.js b/node_modules/drizzle-orm/pg-core/query-builders/query-builder.js
index ab4959f..cad7171 100644
--- a/node_modules/drizzle-orm/pg-core/query-builders/query-builder.js
+++ b/node_modules/drizzle-orm/pg-core/query-builders/query-builder.js
@@ -11,24 +11,35 @@ class QueryBuilder {
     this.dialect = is(dialect, PgDialect) ? dialect : void 0;
     this.dialectConfig = is(dialect, PgDialect) ? void 0 : dialect;
   }
-  $with = (alias, selection) => {
-    const queryBuilder = this;
-    const as = (qb) => {
-      if (typeof qb === "function") {
-        qb = qb(queryBuilder);
-      }
-      return new Proxy(
-        new WithSubquery(
-          qb.getSQL(),
-          selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}),
-          alias,
-          true
-        ),
-        new SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
-      );
+    $with = (alias, selection) => {
+      const queryBuilder = this;
+      let isMaterialized;
+
+      const as = (qb) => {
+        if (typeof qb === "function") {
+          qb = qb(queryBuilder);
+        }
+
+        return new Proxy(
+          new WithSubquery(
+            qb.getSQL(),
+            selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}),
+            alias,
+            true,
+            void 0,
+            isMaterialized
+          ),
+          new SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
+        );
+      };
+
+      const materialized = (shouldBeMaterialized = true) => {
+        isMaterialized = shouldBeMaterialized;
+        return { as };
+      };
+
+      return { as, materialized };
     };
-    return { as };
-  };
   with(...queries) {
     const self = this;
     function select(fields) {
diff --git a/node_modules/drizzle-orm/pg-core/subquery.d.cts b/node_modules/drizzle-orm/pg-core/subquery.d.cts
index 9e60328..c58df7a 100644
--- a/node_modules/drizzle-orm/pg-core/subquery.d.cts
+++ b/node_modules/drizzle-orm/pg-core/subquery.d.cts
@@ -5,14 +5,34 @@ import type { Subquery, WithSubquery, WithSubqueryWithoutSelection } from "../su
 import type { QueryBuilder } from "./query-builders/query-builder.cjs";
 export type SubqueryWithSelection<TSelection extends ColumnsSelection, TAlias extends string> = Subquery<TAlias, AddAliasToSelection<TSelection, TAlias, 'pg'>> & AddAliasToSelection<TSelection, TAlias, 'pg'>;
 export type WithSubqueryWithSelection<TSelection extends ColumnsSelection, TAlias extends string> = WithSubquery<TAlias, AddAliasToSelection<TSelection, TAlias, 'pg'>> & AddAliasToSelection<TSelection, TAlias, 'pg'>;
+interface WithBuilderAs<TAlias extends string> {
+  <TSelection extends ColumnsSelection>(
+    qb: TypedQueryBuilder<TSelection> | ((qb: QueryBuilder) => TypedQueryBuilder<TSelection>),
+  ): WithSubqueryWithSelection<TSelection, TAlias>;
+
+  (
+    qb: TypedQueryBuilder<undefined> | ((qb: QueryBuilder) => TypedQueryBuilder<undefined>),
+  ): WithSubqueryWithoutSelection<TAlias>;
+}
+
+type WithBuilderAsSelection<TSelection extends ColumnsSelection, TAlias extends string> = (
+  qb: SQL | ((qb: QueryBuilder) => SQL),
+) => WithSubqueryWithSelection<TSelection, TAlias>;
 export interface WithBuilder {
-    <TAlias extends string>(alias: TAlias): {
-        as: {
-            <TSelection extends ColumnsSelection>(qb: TypedQueryBuilder<TSelection> | ((qb: QueryBuilder) => TypedQueryBuilder<TSelection>)): WithSubqueryWithSelection<TSelection, TAlias>;
-            (qb: TypedQueryBuilder<undefined> | ((qb: QueryBuilder) => TypedQueryBuilder<undefined>)): WithSubqueryWithoutSelection<TAlias>;
-        };
+  <TAlias extends string>(alias: TAlias): {
+    as: WithBuilderAs<TAlias>;
+    materialized: (shouldBeMaterialized?: boolean) => {
+      as: WithBuilderAs<TAlias>;
     };
-    <TAlias extends string, TSelection extends ColumnsSelection>(alias: TAlias, selection: TSelection): {
-        as: (qb: SQL | ((qb: QueryBuilder) => SQL)) => WithSubqueryWithSelection<TSelection, TAlias>;
+  };
+
+  <TAlias extends string, TSelection extends ColumnsSelection>(
+    alias: TAlias,
+    selection: TSelection,
+  ): {
+    as: WithBuilderAsSelection<TSelection, TAlias>;
+    materialized: (shouldBeMaterialized?: boolean) => {
+      as: WithBuilderAsSelection<TSelection, TAlias>;
     };
+  };
 }
diff --git a/node_modules/drizzle-orm/pg-core/subquery.d.ts b/node_modules/drizzle-orm/pg-core/subquery.d.ts
index affe319..8da13dd 100644
--- a/node_modules/drizzle-orm/pg-core/subquery.d.ts
+++ b/node_modules/drizzle-orm/pg-core/subquery.d.ts
@@ -5,14 +5,34 @@ import type { Subquery, WithSubquery, WithSubqueryWithoutSelection } from "../su
 import type { QueryBuilder } from "./query-builders/query-builder.js";
 export type SubqueryWithSelection<TSelection extends ColumnsSelection, TAlias extends string> = Subquery<TAlias, AddAliasToSelection<TSelection, TAlias, 'pg'>> & AddAliasToSelection<TSelection, TAlias, 'pg'>;
 export type WithSubqueryWithSelection<TSelection extends ColumnsSelection, TAlias extends string> = WithSubquery<TAlias, AddAliasToSelection<TSelection, TAlias, 'pg'>> & AddAliasToSelection<TSelection, TAlias, 'pg'>;
+interface WithBuilderAs<TAlias extends string> {
+  <TSelection extends ColumnsSelection>(
+    qb: TypedQueryBuilder<TSelection> | ((qb: QueryBuilder) => TypedQueryBuilder<TSelection>),
+  ): WithSubqueryWithSelection<TSelection, TAlias>;
+
+  (
+    qb: TypedQueryBuilder<undefined> | ((qb: QueryBuilder) => TypedQueryBuilder<undefined>),
+  ): WithSubqueryWithoutSelection<TAlias>;
+}
+
+type WithBuilderAsSelection<TSelection extends ColumnsSelection, TAlias extends string> = (
+  qb: SQL | ((qb: QueryBuilder) => SQL),
+) => WithSubqueryWithSelection<TSelection, TAlias>;
 export interface WithBuilder {
-    <TAlias extends string>(alias: TAlias): {
-        as: {
-            <TSelection extends ColumnsSelection>(qb: TypedQueryBuilder<TSelection> | ((qb: QueryBuilder) => TypedQueryBuilder<TSelection>)): WithSubqueryWithSelection<TSelection, TAlias>;
-            (qb: TypedQueryBuilder<undefined> | ((qb: QueryBuilder) => TypedQueryBuilder<undefined>)): WithSubqueryWithoutSelection<TAlias>;
-        };
+  <TAlias extends string>(alias: TAlias): {
+    as: WithBuilderAs<TAlias>;
+    materialized: (shouldBeMaterialized?: boolean) => {
+      as: WithBuilderAs<TAlias>;
     };
-    <TAlias extends string, TSelection extends ColumnsSelection>(alias: TAlias, selection: TSelection): {
-        as: (qb: SQL | ((qb: QueryBuilder) => SQL)) => WithSubqueryWithSelection<TSelection, TAlias>;
+  };
+
+  <TAlias extends string, TSelection extends ColumnsSelection>(
+    alias: TAlias,
+    selection: TSelection,
+  ): {
+    as: WithBuilderAsSelection<TSelection, TAlias>;
+    materialized: (shouldBeMaterialized?: boolean) => {
+      as: WithBuilderAsSelection<TSelection, TAlias>;
     };
+  };
 }
diff --git a/node_modules/drizzle-orm/subquery.cjs b/node_modules/drizzle-orm/subquery.cjs
index 7913c04..fb5a7b0 100644
--- a/node_modules/drizzle-orm/subquery.cjs
+++ b/node_modules/drizzle-orm/subquery.cjs
@@ -25,14 +25,15 @@ module.exports = __toCommonJS(subquery_exports);
 var import_entity = require("./entity.cjs");
 class Subquery {
   static [import_entity.entityKind] = "Subquery";
-  constructor(sql, fields, alias, isWith = false, usedTables = []) {
+  constructor(sql, fields, alias, isWith = false, usedTables = [], isMaterialized) {
     this._ = {
       brand: "Subquery",
       sql,
       selectedFields: fields,
       alias,
       isWith,
-      usedTables
+      usedTables,
+      isMaterialized
     };
   }
   // getSQL(): SQL<unknown> {
diff --git a/node_modules/drizzle-orm/subquery.d.cts b/node_modules/drizzle-orm/subquery.d.cts
index 4ea85a0..cce3e01 100644
--- a/node_modules/drizzle-orm/subquery.d.cts
+++ b/node_modules/drizzle-orm/subquery.d.cts
@@ -11,8 +11,16 @@ export declare class Subquery<TAlias extends string = string, TSelectedFields ex
         alias: TAlias;
         isWith: boolean;
         usedTables?: string[];
+        isMaterialized?: boolean;
     };
-    constructor(sql: SQL, fields: TSelectedFields, alias: string, isWith?: boolean, usedTables?: string[]);
+    constructor(
+        sql: SQL,
+        fields: TSelectedFields,
+        alias: string,
+        isWith?: boolean,
+        usedTables?: string[],
+        isMaterialized?: boolean,
+    );
 }
 export declare class WithSubquery<TAlias extends string = string, TSelection extends Record<string, unknown> = Record<string, unknown>> extends Subquery<TAlias, TSelection> {
     static readonly [entityKind]: string;
diff --git a/node_modules/drizzle-orm/subquery.d.ts b/node_modules/drizzle-orm/subquery.d.ts
index e5e7ca8..66760be 100644
--- a/node_modules/drizzle-orm/subquery.d.ts
+++ b/node_modules/drizzle-orm/subquery.d.ts
@@ -11,8 +11,16 @@ export declare class Subquery<TAlias extends string = string, TSelectedFields ex
         alias: TAlias;
         isWith: boolean;
         usedTables?: string[];
+        isMaterialized?: boolean;
     };
-    constructor(sql: SQL, fields: TSelectedFields, alias: string, isWith?: boolean, usedTables?: string[]);
+    constructor(
+        sql: SQL,
+        fields: TSelectedFields,
+        alias: string,
+        isWith?: boolean,
+        usedTables?: string[],
+        isMaterialized?: boolean,
+    );
 }
 export declare class WithSubquery<TAlias extends string = string, TSelection extends Record<string, unknown> = Record<string, unknown>> extends Subquery<TAlias, TSelection> {
     static readonly [entityKind]: string;
diff --git a/node_modules/drizzle-orm/subquery.js b/node_modules/drizzle-orm/subquery.js
index 75d4970..61fb34f 100644
--- a/node_modules/drizzle-orm/subquery.js
+++ b/node_modules/drizzle-orm/subquery.js
@@ -1,14 +1,15 @@
 import { entityKind } from "./entity.js";
 class Subquery {
   static [entityKind] = "Subquery";
-  constructor(sql, fields, alias, isWith = false, usedTables = []) {
+  constructor(sql, fields, alias, isWith = false, usedTables = [], isMaterialized) {
     this._ = {
       brand: "Subquery",
       sql,
       selectedFields: fields,
       alias,
       isWith,
-      usedTables
+      usedTables,
+      isMaterialized
     };
   }
   // getSQL(): SQL<unknown> {
